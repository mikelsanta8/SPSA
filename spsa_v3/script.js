const rev=[...document.querySelectorAll('.reveal')];const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('on')}),{threshold:.08});rev.forEach(x=>io.observe(x));
addEventListener('scroll',()=>{document.querySelector('.progress').style.width=(scrollY/(document.documentElement.scrollHeight-innerHeight)*100)+'%'});
const hero=document.querySelector('.hero');if(matchMedia('(pointer:fine)').matches){hero.addEventListener('mousemove',e=>{const x=(e.clientX-innerWidth/2)/innerWidth,y=(e.clientY-innerHeight/2)/innerHeight;document.querySelectorAll('.layer').forEach((el,i)=>el.style.transform=`translate(${x*(i+1)*14}px,${y*(i+1)*10}px)`)})}
const modal=document.querySelector('.modal'),mp=modal.querySelector('p');document.querySelectorAll('[data-text]').forEach(b=>b.addEventListener('click',()=>{mp.textContent=b.dataset.text;modal.classList.add('open')}));modal.querySelector('button').onclick=()=>modal.classList.remove('open');modal.addEventListener('click',e=>{if(e.target===modal)modal.classList.remove('open')});
